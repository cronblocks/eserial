<Page
    x:Class="ESerial.App.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ESerial.App.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <!-- Rows and Columns Definition -->
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" MinHeight="50" MaxHeight="80" />
            <RowDefinition Height="8*" MinHeight="200" />
            <RowDefinition Height="1*" MinHeight="30" MaxHeight="50" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" MinWidth="180" MaxWidth="200" />
            <ColumnDefinition Width="8*" />
        </Grid.ColumnDefinitions>
        
        <!-- Left Options Bar -->
        <StackPanel
            Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" />
        
        <!-- Top Options Bar -->
        <StackPanel
            Grid.Row="0" Grid.Column="1" />
        
        <!-- Main Text Area -->
        <TextBlock
            Grid.Row="1" Grid.Column="1" />

        <!-- Bottom Messages Bar -->
        <TextBlock
            Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" />

    </Grid>
</Page>
