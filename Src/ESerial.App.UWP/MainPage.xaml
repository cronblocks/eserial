<Page
    x:Class="ESerial.App.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ESerial.App.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <!-- Rows and Columns Definition -->
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" MinHeight="30" MaxHeight="50" />
            <RowDefinition Height="8*" MinHeight="200" />
            <RowDefinition Height="1*" MinHeight="30" MaxHeight="40" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" MinWidth="200" MaxWidth="250" />
            <ColumnDefinition Width="8*" />
        </Grid.ColumnDefinitions>

        <!-- Left Options Bar -->
        <StackPanel x:Name="LeftOptionsPanel"
            Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Background="#F9F3F3F0" />

        <!-- Top Options Bar -->
        <StackPanel x:Name="TopOptionsPanel"
            Grid.Row="0" Grid.Column="1" Background="#F9FDFDEA" />

        <!-- Main Text Area -->
        <TextBox x:Name="MainTextBox" Grid.Row="1" Grid.Column="1" TextWrapping="Wrap" Text="TextBox\r\nABC" FontFamily="Lucida Console" Foreground="#FF222222" Background="#66D2E6D5" HorizontalContentAlignment="Left" ScrollViewer.HorizontalScrollBarVisibility="Auto" VerticalContentAlignment="Top" BorderBrush="#66E6E6E6" SelectionHighlightColor="#FFD67B2C" SelectionHighlightColorWhenNotFocused="#FFFDC491" FontSize="14"/>
        
        <!-- Bottom Messages Bar -->
        <TextBlock x:Name="MessageTextBlock" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" TextWrapping="Wrap" Text="TextBlock" FocusVisualPrimaryBrush="#FF27003A" Foreground="#FF220021" FontSize="12" Margin="3,3,3,3" VerticalAlignment="Bottom" FontWeight="Bold"/>
        
    </Grid>
</Page>
